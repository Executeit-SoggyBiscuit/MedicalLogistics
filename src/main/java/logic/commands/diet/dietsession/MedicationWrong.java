package logic.commands.diet.dietsession;

import Location.Medication;
import logic.commands.Command;
import storage.Storage;

import java.util.ArrayList;

public class MedicationWrong extends Command {
    public void execute(String input, ArrayList<Medication> foodList, Storage storage, Integer index) {
    }
}
